Bootstrap: docker
From: alpine

%setup
   cp hello.py ${SINGULARITY_ROOTFS}/

%post
   ls

   apk update
   apk add python3.6

   #install python to the container
   ./configure
   make
   make install
   ls
